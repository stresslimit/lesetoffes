
<hr>

<div class="container footer">

  <div class="row">
    <div class="col-sm-8">
      <nav>
        {% for link in linklists.footer.links %}
          <a href="{{ link.url }}"{% if link.active %} class="active"{% endif %}>{{ link.title }}</a>
        {% endfor %}
      </nav>
    </div>

    <div class="col-sm-4 text-right">
      <nav class="social-links">
        <a href="http://facebook.com/Les%20%C3%89toffes"><img src="{{ 'facebook.svg' | asset_url }}"></a>
        <a href="http://instagram.com/lesetoffes"><img src="{{ 'instagram.svg' | asset_url }}"></a>
      </nav>
    </div>
  </div>

</div>
