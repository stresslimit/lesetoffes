
<div class="container">

  <h1>Searching for “{{ search.terms }}”</h1>

  {% if search.results_count > 0 %}

    {% for product in search.results %}
      {% if product.available %}
        {% include 'product-card' %}
      {% endif %}
    {% endfor %}

  {% else %}

    <h3>Sorry love, no luck this time</h3>

  {% endif %}

</div>
